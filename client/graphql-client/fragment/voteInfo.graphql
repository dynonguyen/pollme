fragment voteFullInfo on Vote {
	_id
	ownerId
	title
	desc
	type
	createdAt
	updatedAt
	endDate
	allowAddOption
	isIPDuplicationCheck
	isLoginRequired
	isReCaptcha
	isShowResult
	isShowResultBtn
	maxChoice
	maxScore
	maxVote
	totalComment
	totalVote
	tags {
		name
		slug
	}
	answers {
		id
		label
		photo
		voteList {
			rank
			score
			userInfo {
				ip
				name
				userId
			}
		}
	}
	owner {
		avt
		name
	}
}
